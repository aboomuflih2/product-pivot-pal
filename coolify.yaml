# Coolify deployment configuration
build:
  dockerfile: Dockerfile
  context: .

healthcheck:
  enabled: true
  path: /
  interval: 30
  timeout: 10

resources:
  limits:
    memory: 512M
    cpu: 0.5
